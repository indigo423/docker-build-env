node {
  // Mark the code checkout 'stage'....
  stage 'Stage Checkout'
  
  git url: 'https://github.com/opennms/opennms'
  sh 'docker run -v ./opennms:/usr/src/opennms /fullbuild.sh -DskipTests=true'
}
